/* Compatibilidad: el HTML actual conserva esta referencia. 
   La carga principal está en script.js y usa database/invitados.json.
   Este archivo no modifica estilos ni estructura. */
(function () {
  'use strict';
  // Si script.js ya cargó los datos, no hacemos nada.
  // La referencia se conserva para no modificar el HTML original.
})();
